import { test, expect } from '@playwright/test';

// Test placeholder - will be expanded in the next commit

test.describe('EPAM navigation tests', () => {
  test('placeholder', async ({ page }) => {
    // placeholder
  });
});